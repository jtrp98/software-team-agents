# Backend-engineer rationale

The Target's deterministic `.agent-team/config.yaml` `stack:` block is authoritative for profile, tooling, commands, and paths. The role implements that resolved stack and existing repository conventions; choosing or introducing another stack remains a human decision. The prompt keeps its implementation boundaries, contract fidelity, and stop-and-route judgment.
