{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://agentclaude.local/schemas/review.schema.json",
  "title": "review.md structural summary (T53)",
  "description": "Validates the structural summary orchestrator/src/docs/docStructure.ts extracts from a review.md — presence of the sections .claude/agents/qa-engineer.md's Output template requires, plus at least one Review Outcome (T51 reads its `**Status:**` line to generate status.md).",
  "type": "object",
  "additionalProperties": false,
  "required": ["hasOpenIssues", "hasVerificationSummary", "hasChangeLog", "reviewOutcomeCount"],
  "properties": {
    "hasOpenIssues": { "const": true, "description": "## Open Issues — all phases" },
    "hasVerificationSummary": { "const": true, "description": "## Verification Summary (current round)" },
    "hasChangeLog": { "const": true, "description": "## Change Log" },
    "reviewOutcomeCount": { "type": "integer", "minimum": 1, "description": "count of `## Review Outcome — <phase>` sections — a review.md with none has never recorded a verdict" }
  }
}
