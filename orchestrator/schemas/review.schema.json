{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://software-team-agents.local/schemas/review.schema.json",
  "title": "review.md structural summary (V13 TASK-006)",
  "description": "Validates the structural summary orchestrator/src/docs/docStructure.ts extracts from a review.md — the sections .claude/agents/reviewer.md's Output template requires. parseReviewReport() reads the current round's literal verdict line; a review.md without one reads as FAIL.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "hasOpenFindings",
    "hasReviewed",
    "reviewRoundCount",
    "verdictLineCount"
  ],
  "properties": {
    "hasOpenFindings": {
      "const": true,
      "description": "## Open Findings — all phases"
    },
    "hasReviewed": {
      "const": true,
      "description": "## Reviewed — the files the reviewer actually read"
    },
    "reviewRoundCount": {
      "type": "integer",
      "minimum": 1,
      "description": "count of `## Review Round <n> — <task-id>` sections"
    },
    "verdictLineCount": {
      "type": "integer",
      "minimum": 1,
      "description": "count of literal `**Verdict:** ✅ Approved` / `**Verdict:** ❌ Changes requested` lines"
    }
  }
}
