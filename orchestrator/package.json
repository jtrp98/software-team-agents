{
  "name": "@software-team-agents/orchestrator",
  "version": "0.3.0",
  "private": true,
  "description": "Automates the software-team-agents pipeline's opt-in autonomous mode by driving the real .claude/agents/*.md subagents via the claude CLI",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "sta-orchestrate": "dist/cli.js"
  },
  "scripts": {
    "build": "node scripts/clean-dist.mjs && tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "orchestrate": "npm run build --silent && node dist/cli.js",
    "build:templates": "npm run build --silent && node dist/cli.js --build-templates templates --project-root .."
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
