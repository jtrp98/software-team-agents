{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell|Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || exit $?; export CLAUDE_PROJECT_DIR=\"$root\"; node \"$root/.codex/hooks/block-git.js\"",
            "commandWindows": "$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:CLAUDE_PROJECT_DIR = $root; node (Join-Path $root '.codex/hooks/block-git.js'); exit $LASTEXITCODE"
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || exit $?; export CLAUDE_PROJECT_DIR=\"$root\"; node \"$root/.codex/hooks/block-outside-repo.js\"",
            "commandWindows": "$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:CLAUDE_PROJECT_DIR = $root; node (Join-Path $root '.codex/hooks/block-outside-repo.js'); exit $LASTEXITCODE"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || exit $?; export CLAUDE_PROJECT_DIR=\"$root\"; node \"$root/.codex/hooks/block-doc-rewrite.js\"",
            "commandWindows": "$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:CLAUDE_PROJECT_DIR = $root; node (Join-Path $root '.codex/hooks/block-doc-rewrite.js'); exit $LASTEXITCODE"
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || exit $?; export CLAUDE_PROJECT_DIR=\"$root\"; node \"$root/.codex/hooks/block-path-permissions.js\"",
            "commandWindows": "$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:CLAUDE_PROJECT_DIR = $root; node (Join-Path $root '.codex/hooks/block-path-permissions.js'); exit $LASTEXITCODE"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || exit $?; export CLAUDE_PROJECT_DIR=\"$root\"; node \"$root/.codex/hooks/require-green-before-stop.js\"",
            "commandWindows": "$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:CLAUDE_PROJECT_DIR = $root; node (Join-Path $root '.codex/hooks/require-green-before-stop.js'); exit $LASTEXITCODE"
          },
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || exit $?; export CLAUDE_PROJECT_DIR=\"$root\"; node \"$root/.codex/hooks/block-secret-leak.js\"",
            "commandWindows": "$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:CLAUDE_PROJECT_DIR = $root; node (Join-Path $root '.codex/hooks/block-secret-leak.js'); exit $LASTEXITCODE"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || exit $?; export CLAUDE_PROJECT_DIR=\"$root\"; node \"$root/.codex/hooks/require-green-before-stop.js\"",
            "commandWindows": "$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:CLAUDE_PROJECT_DIR = $root; node (Join-Path $root '.codex/hooks/require-green-before-stop.js'); exit $LASTEXITCODE"
          },
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || exit $?; export CLAUDE_PROJECT_DIR=\"$root\"; node \"$root/.codex/hooks/block-secret-leak.js\"",
            "commandWindows": "$root = git rev-parse --show-toplevel; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:CLAUDE_PROJECT_DIR = $root; node (Join-Path $root '.codex/hooks/block-secret-leak.js'); exit $LASTEXITCODE"
          }
        ]
      }
    ]
  }
}
