# dependencies / build / test artifacts
node_modules/
dist/
build/
coverage/
.vite/
*.tsbuildinfo
*.log

# T90 — build artifact of this repo: a snapshot of the framework template files +
# manifest.json, generated by `npm run build:templates` (orchestrator/src/packaging/).
# Never hand-edited, never the source of truth (that stays at .claude/agents/, contracts/,
# workflows/, policies/, stacks/, etc.) — regenerate it, don't commit it.
/templates/
.workflow/

*.tgz

# internal planning docs (T-checklists/handoffs) � local only
planning/
